#!/usr/bin/env node

const chalk = require('chalk')

console.log(chalk.yellow(
  '\n' +
  '  We are slimming down kdu-cli to optimize the initial installation by ' +
  'removing the `kdu build` command.' +
  '\n'
))
